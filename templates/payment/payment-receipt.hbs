<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Payment Receipt</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        font-size: 12px;
        margin: 0;
        padding: 16px;
      }
      .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 12px;
      }
      .company-details {
        line-height: 1.4;
      }
      .title {
        text-align: center;
        font-size: 16px;
        font-weight: bold;
        margin: 12px 0;
        text-transform: uppercase;
      }
      .section {
        margin-bottom: 10px;
      }
      .label {
        font-weight: bold;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 8px;
      }
      th,
      td {
        border: 1px solid #444;
        padding: 4px 6px;
        text-align: left;
      }
      .footer {
        margin-top: 24px;
        display: flex;
        justify-content: space-between;
        font-size: 11px;
      }
      .signatory {
        text-align: right;
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <div class="company-details">
        <div class="label">{{company.name}}</div>
        {{#if company.address_line_1}}<div>{{company.address_line_1}}</div>{{/if}}
        {{#if company.address_line_2}}<div>{{company.address_line_2}}</div>{{/if}}
        <div>
          {{company.city}}, {{company.state}} {{company.pin_code}}
        </div>
        {{#if company.phone}}<div>Contact No.: {{company.phone}}</div>{{/if}}
        {{#if company.email}}<div>Email Id: {{company.email}}</div>{{/if}}
      </div>
      {{#if company.logo_data_url}}
      <div>
        <img src="{{company.logo_data_url}}" alt="Logo" style="max-height: 60px;" />
      </div>
      {{/if}}
    </div>

    <div class="title">Payment Receipt</div>

    {{#if order}}
    <div class="section">
      <div class="label">Customer:</div>
      <div>{{order.customer_name}}</div>
      <div>{{order.address}}</div>
      {{#if order.phone}}<div>Phone: {{order.phone}}</div>{{/if}}
    </div>
    {{/if}}

    <div class="section">
      <div><span class="label">Payment Date:</span> {{payment.date_of_payment}}</div>
      {{#if order}}
      <div><span class="label">Order No:</span> {{order.order_number}}</div>
      {{/if}}
      {{#if payment.receipt_number}}
      <div><span class="label">Receipt No:</span> {{payment.receipt_number}}</div>
      {{/if}}
    </div>

    <div class="section">
      <table>
        <thead>
          <tr>
            <th>Description</th>
            <th>Payment Mode</th>
            <th>Payment Amount</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>
              {{#if order}}Project Capacity: {{order.capacity}}{{else}}Payment{{/if}}
            </td>
            <td>{{payment.payment_mode_name}}</td>
            <td>{{payment.payment_amount}}</td>
          </tr>
        </tbody>
      </table>
    </div>

    <div class="section">
      <div><span class="label">In Words:</span> {{payment.payment_amount_in_words}} only</div>
      {{#if payment.transaction_cheque_date}}
      <div>
        <span class="label">Cheque / Transaction Date:</span> {{payment.transaction_cheque_date}}
      </div>
      {{/if}}
      {{#if payment.transaction_cheque_number}}
      <div>
        <span class="label">Cheque / Transaction No.:</span> {{payment.transaction_cheque_number}}
      </div>
      {{/if}}
      {{#if payment.bank_name}}
      <div>
        <span class="label">Bank Name:</span> {{payment.bank_name}}
      </div>
      {{/if}}
    </div>

    <div class="signatory">
      <div>For, {{company.name}}</div>
      <div style="margin-top: 32px;">(Authorized Signatory)</div>
    </div>

    <div class="footer">
      <div>Note: This is a system-generated receipt.</div>
      <div>Generated on: {{generated_at}}</div>
    </div>
  </body>
</html>

